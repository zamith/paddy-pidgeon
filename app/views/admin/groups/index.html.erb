<h1 class="page-header"><%= t("admin.groups.index.title") %></h1>

<ul id="groups">
  <% @groups.each do |group| %>
    <li><%=  %></li>
  <% end %>
</ul>

<%= link_to t('admin.groups.new.title'), new_admin_group_path %>
<%= link_to t('admin.groups.back'), root_path %>
